// setTimeout(() => {
//     alert("HELLO");
// }, 2000);

// returns timer id

// syntax:
// setTimeout(funcName, time, funcArg1, funcArg2 ....)

const sum = (x, y, z) => {
    const sum = x + y + z;
    alert(`Sum: ${sum}`);
}

setTimeout(sum, 2500, 2, 5, 8);

// setInterval() format is same
// setTimeout() : it runs the function once after a given time period (just once)
// setInterval() : it runs the function repeatedly after the given interval of time


// setTimeout and setInterval both are ASYNCHRONOUS !!


btn.addEventListener('click', function(e) {})

